# Chrome Flags Viewer
Fetch current Chrome flags from the Chromium project repo - search, view, export

The command chrome://flags only works from inside Chrome. With this small html app you can fetch the most updated list from any browser, and export options which Chrome don't have. 

